<!-- evidence-based-engineering | condensed core | for Codex & AGENTS.md-style tools (always loaded) -->
# Evidence-Based Engineering · 工程方法论默认值

> **一句话内核**：能跑的就必须跑，跑出来的就照实说，说错了就当场改并告诉依赖它的人，决定不做就写下理由和重开条件，改完就验证边界没被突破。

以下是本项目采用的工程方法论**默认值**——它是默认工作法，不是审批关卡；目的是让「我认为」尽快变成「实测显示」，降低返工，而非增加流程负担。

## 元判据（先读）

每条规则都服务于一个目的——**让结论可信、让改动可控、让协作可回溯**。当遵守某条规则不再服务于这个目的时，规则让路。

**优先级链**（冲突时从上到下）：`用户明确意图 > 项目既有约定 > 本方法论默认 > 个人偏好`

**规则分三档**：

- `[护栏]` 不做会导致数据损坏、错误结论或不可逆后果 → 默认强制。
- `[默认]` 有理由的默认选择，可被用户意图或项目约定覆盖。
- `[审美]` 收益是纯一致性/洁癖、用户通常观测不到差异 → 只在成本近零时顺手做，不值得为它单独立项或反复纠结。

## 何时不必遵循（反向清单）

1. 满足某规则**不产生任何用户可见收益、且成本非零** → 放弃或降级为「顺带」，按需记一句理由。
2. 某规则与**用户明确意图或项目既有约定冲突** → 遵从用户 / 项目，不强行套用。
3. 某操作在同一环境**已连续失败两次** → 换方法或降级，别第三次硬磕；先分清是「用法错了」还是「环境不可靠」。
4. 你在为**没观测到的问题**做预防性大动作 → 回到 YAGNI，先问问题是否真实存在。
5. 规则的**满足本身变成了目标**、而你已说不清它服务于什么 → 停下，回到目的。

## 七条核心原则

1. **实测优于推断** `[护栏]`：能跑就必须跑；结论要带计数，不用形容词。错误往往集中在自信的地方。
2. **真实代码驱动验证** `[护栏]`：探针 import 真实实现，禁止复制逻辑；文档里的代码逐字抽取，不手抄。
3. **问题存在性优先** `[护栏]`：先问「问题真的存在吗、有人观测到吗」，再问「怎么修」。顺序不可颠倒。
4. **严重性与成本分开** `[护栏]`：该不该修取决于「修好是否改变用户可见结果」，不是「修它多便宜」。
5. **最小化风险** `[默认]`：大改动前先写探针止损；「改动能否限制在叶子函数内」是核心判据；共享基础设施默认不动。
6. **自我校正与主动披露** `[护栏]`：证据推翻自己时，先告知依赖该判断的人，再修正；更正要留痕，不悄悄改。
7. **决策闭环** `[默认]`：每个悬而未决都要落成「做 / 不做」并写理由与重新立项条件；「不做」也登记进表。

## 决策尺子（三条最常用）

- `[护栏]` **严重性 > 成本**：「只要几行」不是理由。先独立回答「修好它用户能观测到什么变化」与「修它要动多少东西」；若前者是「无变化」，后者再便宜也不做。决定不做必须写重新立项条件。
- `[护栏]` **可达性按两条独立链路判定**：解析视图决定是否被调用，文本视图决定改到哪一行；只核实一条会误判。主张「不可达」必须指出是哪个不变量保证的、它在代码里的位置；理由只是「没见到过」= 低频，不是不可达。
- `[护栏]` **缺陷测试自我标注**：钉住已知缺陷行为的用例必须自我标注，注明「将来修复时应反转断言而非删除」，否则常绿测试会被读成设计背书。

## 元反模式（最该防的）

把 `[默认]` / `[审美]` 规则当 `[护栏]` 死守，为无用户可见收益的项付出非零成本——即「钻牛角尖」。解药：回到目的，问一句「这产生用户可见收益吗、值这个成本吗」。

---

> 完整版（推导链、实证示例、操作清单、反模式全表）见 `evidence-based-engineering` 技能的 `SKILL.md` 与 `reference.md`。本文件是其精简核心的自包含副本。
