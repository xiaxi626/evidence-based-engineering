# Evidence-Based Engineering · 详细参考（reference）

> **本文件定位**：这是 `evidence-based-engineering` 技能的详细参考，承载完整原理、说明性实证示例与反模式清单；可操作的速查清单见同目录 `SKILL.md`。以下为跨项目通用版《通用协作与工程验证方法论》全文，与 `SKILL.md` 配套阅读。

> **来源**：从一次完整的「外部评估 → 优化方案 → 逐任务评审 → 实施」工程流程中回溯提炼。推导链为：**外部评估文档** →（逐条核实）→ **优化方案** →（逐任务评审）→ **本方法论**。
>
> **性质**：这不是一份先验规范，而是一组**在真实决策点上被用过、并且有实证结果**的工作法。为便于跨项目复用，本文已剥离具体项目的标识信息（提交号、文件名、行号、任务编号、精确计数），只保留原则、决策逻辑、验证步骤与反模式，并把真实案例改写为**说明性示例**。
>
> **定位**：默认工作法，不是审批关卡。它的目的是让「我认为」尽快变成「实测显示」，而不是增加流程负担。

---

## 规则档位与元判据（先读）

本参考里的所有规则都分三档，执行强度不同；可操作的反向清单见 `SKILL.md`「元判据」小节。

- `[护栏]`：不做会导致数据损坏、错误结论或不可逆后果，默认强制。
- `[默认]`：有理由的默认选择，可被用户意图或项目既有约定覆盖。
- `[审美]`：收益是纯一致性/洁癖、用户通常观测不到差异，只在成本近零时顺手做。

**优先级链**：`用户明确意图 > 项目既有约定 > 本技能默认 > 个人偏好`。

**为什么需要分档**：把经验固化成清单时，天然有两个失败模式——把软偏好当硬约束、脱离产生规则的语境（规则一旦离开它被总结出来的那个项目，就容易被当成放之四海的律法）。分档 + 优先级链 + 反向清单（何时不必遵循）是防止方法论自身退化成教条的护栏。

**如何判断某条规则此刻该不该遵守**：回到内核那句——能不能产生用户可见的收益，值不值这个成本。若遵守它已不服务于「让结论可信、让改动可控、让协作可回溯」这个目的，规则让路。特别地：

- 一条 `[审美]` 规则（如统一的换行符 / 编码风格）若与所处环境的默认行为相冲突、且无用户可见收益，应顺其自然，而不是反复手动纠正——环境往往会自动把它改回去，纠正的成本被反复支付却买不到任何持久收益。
- 一条 `[默认]` 规则遇到项目已有约定时，从项目约定。
- 只有 `[护栏]` 规则（会造成不可逆损害的那些）才值得在任何环境下坚持。

---

## 第 0 章 方案是怎么来的：推导链

### 0.1 起点不是生成，是核查

一份可靠的优化方案，通常在开头就声明它的真实来源，形如：

> 基于对某外部评估文档的核查，过滤掉不准确的假设（例如断言「缺少某份本应存在的文档」，而它其实已存在）和过早扩张的建议（例如超出当前阶段范围的平台/功能扩展），聚焦于真实存在的健壮性问题、体验改进和覆盖缺口。

也就是说，方案不是从「一个项目通常应该有什么」倒推出来的，而是走了四步：

1. **拿到一份外部评估**——它可能来自他人、来自某个 AI 工具，或来自一次代码审计
2. **逐条到仓库/代码里核实**
3. **过滤掉两类内容**：事实性错误、价值判断上的过早扩张
4. **剩下的才成为任务**

> 这条推导链应当是**物理可核对**的：源文档、核查产物（方案）、以及由方案沉淀出的方法论最好同处一地。方案的每个任务都应能回溯到源文档的某条建议，以及核实该建议时读到的具体材料。

方案的自我约束也应来自核实，而非凭空设定。例如方案在开头写明技术栈与「不新增运行时依赖、不破坏既有安装/集成流程」这类硬约束后，**这些约束会贯穿全部任务**，并在评审中成为否决某些建议的直接依据（如否决「把某个自研解析器替换为第三方库」）。

### 0.2 三步过滤

核实阶段过滤掉的内容，通常落入三类。下面用通用示例说明：

| 过滤类型 | 示例条目 | 核实依据（示例） |
|---|---|---|
| 事实性错误 | 「项目缺少某份贡献/说明文档」 | 仓库里其实已存在该文档，甚至有多语言版本 |
| 过早扩张 | 支持更多第三方平台、增加 shell 补全等非核心能力 | 项目仍处早期；现有简写/手动方式已可覆盖，社区可后续按需补 |
| 隐含约束 | 不引入新依赖 | 现有唯一运行时依赖已足够，方案全程未新增 |

**关键点**：被过滤掉的条目**没有被删除**，而是进了「不做的事项」表并附理由（见 §4.2）。

**核实用到的材料**（这也是「方案来自阅读项目本身」的具体含义）通常包括：各类说明文档（README、贡献指南、变更日志）、架构与设计说明、既有的验证/测试记录、CI 配置、议题模板、构建与依赖清单、代码规范配置，以及源码与测试全部。核实过程中的两个常见收获会直接影响后续任务：

- **既有验证文档的存在**，会决定新任务的**验收形式**（例如「实施后配套产出一份验证记录」）。
- **构建/测试脚本的组织方式**，会成为**改动面评估的关键约束**（例如「测试脚本是硬编码的全量列表，新增测试文件必须登记，而往已登记文件里加用例则不必」——这一条会显著改变两个看似等价方案的代价）。

### 0.3 递归适用：同一把尺子也要量方案自己

这是整套流程最关键的一步。方案定稿后评审并没有停止，而是把 §0.1 的核查纪律**反过来用在方案本身**。一份初看合理的任务清单，逐任务核查后，其条目的最终去向往往分布在以下几种：

| 去向 | 含义 | 典型依据（示例） |
|---|---|---|
| ✅ 保留 → 实施 | 问题真实、方案正确 | 通过实测确认收益与正确性 |
| ❌ 移除 | 问题不存在，或方案比问题更危险 | 误判会自纠正、结局恒正确；或属 YAGNI；或核查发现原实现反而引入数据丢失陷阱 |
| ⚠️ 收敛 | 问题真实但方案过大 | 收敛为「最小外科版」，只改一个叶子函数；用差分实测证明回归为零 |
| 🔧 打补丁 | 方案基本成立但有漏项 | 差分实测发现原方案遗漏了某类输入的处理，需补护栏 |
| ➕ 新增 | 评审中才暴露的项 | 核查发现某处逻辑**零测试覆盖**，且带着一个已确认可达的缺陷在跑 |

**账目本身的含义**：一份看起来合理的计划，逐任务核查后往往有**相当比例**需要修正——被移除、被收敛、被打补丁，甚至会新增评审中才发现的任务；「原样未被触动」的条目可能只占一半左右。

这个数字（无论具体是多少）就是方法论的核心论据：**核查不是对方案作者的不信任，是方案应有的最后一道工序。**

### 0.4 项目层面的同一现象

「扩大观测面」本身会立刻暴露此前不可见的问题。一个典型链条是：某任务把 CI 从单一平台扩展到多平台矩阵，并补了跨平台集成测试；**紧接着的下一个提交**，往往就是修复一个**只在新平台 + 特定运行时版本上**才出现的断言失败。

这样的失败在旧观测面下**完全不可见**。这不是谁写错了，而是**观测面不够**。扩大观测面本身就是一项有回报的投资——它把「潜伏的平台差异」提前变成「可见的红灯」。

---

## 第 1 章 事实层：怎么知道一件事是真的

### 1.1 实测优于推断

**定义**：任何关于代码行为的断言，只要能跑，就必须跑。「零回归」「经核验」「应该不会有问题」这类表述在写入文档前必须升级为**带计数的实测结论**。

**内涵**：
- 推断的错误率不为零，而且**错误往往集中在自信的地方**
- 实测的成本通常远低于推断出错后的返工成本
- 实测结果可以写进文档成为**证据**；推断只能写进文档成为**主张**

**说明性示例**（推断 vs 实测的典型落差）：

| 场景 | 推断会说 | 实测显示 |
|---|---|---|
| 声称「零回归」 | 「现有用例逐条核验通过」 | fixture 差分后发现约一半行为改变；既存测试的输入全部落在「未变」一侧，但**改变项里藏着原方案没想到的输入** |
| 声称「已覆盖」 | 原方案未提及某类输入 | 形如 `--flag=false` 的布尔标志被误解析为真值，绕过了本应触发的确认 |
| 声称「预期失败」 | 「新用例应该会红」 | 旧实现大部分用例失败、新实现全绿；而那唯一「常绿」的旧用例其实是回归护栏，不是新功能 |
| 声称「更彻底」 | 「完整重写比最小改动更干净」 | 逐字节差分证明最小版回归为零，完整版反而触及共享链路、风险更大 |
| 声称「不可达」 | 「当前不会触发」 | 实跑探针证明**可达**：某入口标志会被置位、正文被改写 |

**如何复用**：
1. 准备一组 fixture，**必须包含现有测试用到的全部输入**——这是证明零回归的唯一办法
2. 新旧实现并排 import，逐 fixture 比较输出（**异常也要捕获后一并比较**，抛错与返回值是两种不同结果）
3. 报告两个计数：行为相同 / 行为改变
4. **改变项逐条归类**为「本次意图内的修复」或「意外变更」。意外变更必须处理，或显式记录为残留并说明不做的理由
5. 把计数写回文档，替换掉形容词

### 1.2 真实代码驱动验证

**定义**：验证一个函数的行为时**import 真实实现**，不要在探针脚本里复制它的逻辑。验证一份文档里的代码时**从文档逐字抽取**，不要手抄。

**内涵**：复制出来的「等价实现」会在你最需要它的时候不等价——而你无法察觉，因为你比较的是自己的抄本和自己的推断。

**说明性示例**：
- 探针直接 `import` 被测的真实源码运行，而不是把那段逻辑抄进脚本。许多「可达性」结论依赖的是**调用方**的行为，而非被抄的那段代码本身——只有跑真实链路才能发现（例如「某入口标志是否会被置位」取决于上游解析器，而非目标正则）。
- 验证「文档里给出的实现」时，用脚本按标题定位 + 围栏切片，从方案文档里**逐字抽出**实现与配套测试，改名后编译运行——而不是照着文档手敲一遍。
- 定稿后**再从已更新的文档里重新抽取跑一次**。这一步证明的是「文档本身可执行正确」，而不只是「我脑子里的版本正确」。

**如何复用**：
1. 探针脚本一律 import 被测源码，**禁止复制实现**
2. 抽取文档代码块后先断言关键标识存在（如函数签名）、条数符合预期，再运行
3. 文档改完后重跑一次抽取——文档和代码会各自漂移，只有重跑能证明它们仍然一致
4. 需要理解跨模块行为时，把**调用链上的每个环节**都读一遍（某个标志「有多严重」，往往只有读到它在下游被怎样消费才能确定）

### 1.3 问题存在性优先于解决方案

**定义**：评审一个任务时，先问「这个问题真的存在吗、有人观测到过吗」，再问「该怎么修」。**顺序不能颠倒。**

**内涵**：一旦开始讨论实现方案，注意力会转移到「怎么做」，从而默认了「该做」。把存在性问题单独拎出来，是唯一能阻止这个滑坡的办法。

**评审用的固定三问**（把事实、价值判断、实施方案强行分开）：

> 分析某任务的**问题存在性**以及**是否应当处理**？如果确实需要，分析**方案是否需要调整**。敲定该任务方案。

**三问带来的典型结论**（示例）：

- **重复代码类**：重复**属实**，但若它是纯展示逻辑、无任何观测到的缺陷，则收益是**审美性**的；当代价是一次提交触达大量文件时 → **移除**。
- **YAGNI 类**：某校验针对的字段只有一个版本、无变更计划 → YAGNI。更关键的是，核查发现原方案的实现（「先备份、再返回空表」）会在版本降级时**静默清空并覆盖数据** → 这不是修复，是引入数据丢失陷阱 → **移除**，并在「不做」表写明「若将来引入新版本，应改为 fail-safe（拒绝 + 提示升级、绝不自动覆盖）」。
- **自纠正类**：某处误判会自纠正、结局恒正确，真正要紧的不变量已被别的测试覆盖 → **移除**。

**如何复用**：
1. 对每个候选任务强制回答：「有没有人观测到这个缺陷？还是只是看起来不优雅？」
2. 如果答案是后者，检查收益是**功能性**的还是**审美性**的
3. 审美性收益不是不能做，但必须与改动面成比例——用「一次提交触达多少文件」量化代价
4. **核查可能反转结论**：有的原实现比它要修的问题更危险。这种情况必须记录，否则下一个人会重新提出同一个方案

---

## 第 2 章 判断层：怎么决定做不做

### 2.1 严重性与成本分开判断

**定义**：一个缺陷该不该修，取决于**修好它是否改变用户可见结果**，而不是取决于修它有多便宜。

**内涵**：「只要几行」是最危险的论证——它把成本当成了理由。便宜的事情如果不产生收益，做了只是增加改动面和验证负担。

**说明性示例（便宜但不修）**：某处正文污染缺陷，修法已知且只需几行，但**决定不修**，决定性理由是严重性：

- 在它的触发场景里，即使修好污染，**用户可见结果也不变**——触发前提本身就使后续处理失败，产物照样被跳过
- 被改写的那一行位于**每轮都会被丢弃重来的一次性托管产物**里，下次运行即自愈
- 所以修它只买到「不再对可丢弃文件做外观损伤」
- 附带**重新立项条件**：若将来那个「根因缺陷」被立项修复，本项应与其**同一批**处理——只有那时改动才带来用户可见收益

**对照示例（便宜且严重 → 必须做）**：为 CLI 增加 `--flag=value` 语法时，差分实测发现原方案漏了布尔标志的处理——`--flag=false` 会被读成真值，从而**同时绕过两道本应触发的确认护栏**，其终点恰好与本任务要修的「静默批量操作」相同。这类补丁便宜且严重，必须随任务一起做。

**如何复用**：
1. 先独立回答两个问题：「修好它，用户能观测到什么变化？」/「修它要动多少东西？」
2. 如果第一个答案是「没有变化」，第二个答案再便宜也不构成理由
3. 判断「用户可见」时要追到**数据的生命周期**：被改的文件是用户资产还是一次性托管产物？后者会自愈
4. 决定不做时**必须写重新立项条件**（见 §4.1）

### 2.2 最小化风险：先用小探针止损

**定义**：实施一个较大改动前，先写一个最省事的版本跑一遍。它的作用不是交付，而是**暴露你没想到的失败模式**。

**说明性示例**：某次要收敛一处文本改写逻辑。

- 先写一个「更省事的版本」：只做最小修正，不加范围限定
- **实测发现它引入了一个当前代码没有的新 bug**：在整篇文件上匹配某个模式，会改坏正文代码块里的示例内容
- 于是「范围限定」从一个看起来多余的复杂度，变成了**必需品**
- 最终方案收敛为只改**一个叶子函数**，共享的解析器/正则一律不动——因为它们被用在**整条发现/处理链路**上（覆盖多个命令），为一个只在局部运行的重写去改全链路解析器的结构，风险面与收益不成比例

**判据**：改动能否被限制在**叶子函数**内。能，就做；不能，就要重新论证收益是否配得上风险面。

**如何复用**：
1. 大改动前先写一个简化版，目的明确写着「这是探针不是交付物」
2. 探针跑出**新**缺陷时，不要把它当成「简化版的固有问题」忽略掉——它往往指出了完整版的必要组成
3. 用「这次改动触达几个文件 / 几条调用链」量化风险面
4. 共享的基础设施（被多条链路使用的解析器、工具函数）**默认不动**

### 2.3 不为不可达路径写测试

**定义**：可达性判定的依据必须是**可追踪的代码不变量**，而不是「至今没触发过」。

**内涵**：给不可达路径写测试，等于把投机行为固化成规格。将来有人读到这条测试，会认为这个输入是合法场景。

**说明性示例（确实不可达）**：判断某注入路径是否可达时，结论是**不可达**，但理由必须精确：目标函数只在某个分支被调用，传入值恒为上游某个「净化函数」的输出，而该净化函数把非白名单字符整体替换掉，**输出字符集不可能含目标字符**。因此**不为它写测试**，并写明「若将来净化函数被放宽，应在其**所在模块**补不变量测试」。同时砍掉另外两类无信息量的用例：与其他用例重复覆盖的情形、以及任何「修正验收」式断言（那是**修复**的验收标准，写进特征测试会立刻变红）。

**反面教材（可达性误判）**：曾判定某路径「当前不可达」，理由是「某入口标志需要顶层键存在才会被设置」。**这个判定是错的**——上游解析取自结构化解析结果，所以当输入写成另一种形态时该标志**会**被置位；而目标正则的锚点匹配不到那种形态，扫描遂越过边界改写了正文。

**教训**：可达性必须按**两条独立链路**分别判断——**解析视图决定是否被调用，文本视图决定改到哪一行**。只核实其中一条就会误判。

**如何复用**：
1. 主张「不可达」时，必须指出是**哪个不变量**保证了它，并给出该不变量在代码里的位置
2. 如果理由只是「没见到过」，那就不是不可达，只是低频
3. 一个入口标志可能由多种输入形态产生（解析后的值 vs 原始文本），**逐一核实每一种**
4. 不变量被放宽时，测试应该加在**不变量所在的模块**，而不是加在下游消费者

---

## 第 3 章 自我校正层

### 3.1 对自己提出的方案保持更高的怀疑

**定义**：当一项工作是自己提议的，论证义务更重，而不是更轻。

**说明性示例**：在完成一处收敛后，主动提出一个开放项（例如「给某个此前零覆盖的函数补一组特征测试」）。合理的回应不是马上写测试，而是要求先论证：

> 把它单独立项、写进方案。**一定要捋一下确认你想增加的任务是合理的**——我们不想把任务搞得过于复杂，但该做还是得做。

于是先做论证，而不是先动手：

- **为什么值得做**（每条都不是审美性的）：① 它带着一个**已实测确认可达**的缺陷在跑，而相邻的姊妹逻辑刚为**同一触发条件**修掉了同类问题，这个不对称不写成测试后来者看不见；② 方案已明确推迟某些修正，而**推迟的正当前提必须被记录在案**——计划文档会被归档，测试不会；③ 它是任何未来修正的**前置条件**
- **为什么它能保持简单**：不引入红/绿两阶段 → 无实现改动 → 无回归面 → 验收退化为两条（全部用例首跑即绿；除测试与变更日志外不得出现任何其他文件改动）
- **砍掉了什么**：主动列出从一批探针 fixture 收敛到少数几条测试的裁剪过程

**如何复用**：
1. 自己提议的任务，先写「为什么值得做」和「为什么它能保持简单」两段，**再**写实施步骤
2. 主动列出**砍掉了什么**——裁剪记录比范围声明更能证明你想清楚了
3. 纯增量任务（只加测试、只加文档）的验收标准要与修复任务**不同**，不要机械套用红/绿两阶段

### 3.2 实测推翻自己时，立即修正并主动披露

**定义**：当证据推翻了先前给出的判断，第一动作是**告知依赖该判断做决定的人**，第二动作才是修正。

**说明性示例**：当一处「不可达」判定被实测推翻后：

1. **主动披露**：明确告知「我之前给你的一个判断是错的，而你在做范围决定时依赖了它」——因为对方先前的决定部分建立在那个错误判断上，他有权重新决定
2. **把决定权交还**：给出几个选项（修 / 不修 / 扩大范围），而不是自行选择
3. **更正留痕**：把旧判定**原文引用后驳斥**，而不是悄悄改掉。文档里于是同时存在「原判定」（作为被引用的错误）和「这条判定是错的」（作为结论）
4. **连带更正理由错误的正确结论**：若某结论（如「不可达」）是对的、但**理由**是错的（如「同上」），一并更正为真正的不变量依据
5. **更正记忆**：若同一句错误判定已写进长期记忆/文档/注释，检索并更新它，必要时新增「已更正的错误判定」专段

**如何复用**：
1. 判断被证据推翻时，检查**有谁依赖了这个判断做决定**，主动告知
2. 更正要**留痕**：引用原文再驳斥，而不是替换。悄悄修改会让后来者失去「这里曾经错过」的信息，从而可能重犯
3. 一个**错误理由支撑的正确结论**也是问题——理由错了，下次场景变化时结论就会错
4. 错误如果已经写进长期记忆 / 文档 / 注释，**必须一并检索并更正**，否则它会继续误导

### 3.3 计数式自校验：让文档编辑可失败

**定义**：批量编辑文档时写脚本执行，并在**写盘之前**跑一组计数断言。失败即退出，原文件不受损。

**说明性示例**：对方案文档做多轮批量替换，每轮脚本含数十项断言：

- 每处替换的目标串**必须恰好出现 1 次**（否则说明锚点不唯一或已被改过）
- 旧表述归零计数、新表述出现次数
- 行数护栏（期望 = 原行数 + 各编辑预测增量 ± 容差）
- 代码围栏总数为偶数、无 CR、无 BOM、标题数量不变

**踩到的坑（重要，通用）**：

| 坑 | 症状 | 根因 | 修法 |
|---|---|---|---|
| 字符串替换 API 的反向引用 | **所有**计数断言同时以整数倍（如 2×）超标，文档头部被复制多遍 | 新文本里含 `` $` ``（美元符号紧跟反引号）之类的特殊序列，被当成「插入匹配位置之前的全部内容」 | 替换串**一律用回调** `() => newStr` |
| 断言作用域过宽 | 「旧表述已归零」却报剩余 | 新内容为了**驳斥**而原文引用了旧表述 | 给待查串加上下文限定，并**新增正向断言**要求「驳斥引用」各存在 1 次 |
| golden 串跨章节误报 | 期望 1 次、实际多次 | 同一字符串在别的章节作为**输入字面量**合法出现 | 按章节切片后再计数 |
| 自写行对齐算法 | 报出假的增删行数与「某段被改动」 | 简易双指针在改写区附近丢失对齐 | 以版本控制工具的 `diff --numstat` 与 hunk 头为**权威**，核对 hunk 数与替换次数一一对应（相邻编辑会被合并成一个 hunk） |

**讽刺之处**：`` $` `` 这个坑，恰恰可能是同一份方案里刚描述过的某个缺陷。**知道它、写过它、然后踩了它。** 这说明「知道一个陷阱」和「在手上有防它的机制」是两件事——防它的机制是那条**行数护栏断言**，它让损害停在写盘之前。

**如何复用**：
1. 编辑工作区外的文件时（编辑工具可能拒绝），改用工作区内临时脚本 + 提权执行
2. **校验必须置于写盘语句之前**——这一条让所有失败都变成无损失败
3. 替换串一律用回调，无论当前文本里有没有 `$`
4. 断言要限定作用域；写「某表述已归零」前先想想新内容会不会为了驳斥而引用它
5. 行级差分用版本控制工具，不用自己写的算法
6. 临时脚本用后即删，结束时确认版本控制状态干净

---

## 第 4 章 闭环层

### 4.1 决策闭环：每个悬而未决都要落地

**定义**：讨论产生的每个选项，最终必须变成「做」或「不做」并写进文档。**「可选延伸（需另行决定）」不是终态。**

**说明性示例**：当某缺陷的处理方式经决策者拍板后，方案里应做的编辑：

- 新增**首条**理由——把**严重性论证放在成本论证之前**，防止将来有人只看到「便宜、几行」就重新立项
- 把「可选延伸（需另行决定）」改写为「**已决定：不做**——本任务不包含，也不另立任务」
- 附**重新立项条件**：「若将来某根因缺陷被立项修复，本项应与其同一批处理——只有那时改动才带来用户可见收益，且两者共享同一触发条件、宜一并验收」
- 在所有提及该结论的地方**同步**，避免只在一处留痕

一个任务的闭环陈述应包含三要素：**发现了什么** / **已经处理** / **现在的可实施判断**。例如：

> 该任务现在的结论是：方案原本会带着一个自己新造的静默错误路径上线，已补齐；补完之后它可以实施了。

**如何复用**：
1. 「不做」的决定要写明**判据**（如「修正能否限制在叶子函数内」+「修好是否改变用户可见结果」）
2. 要写明**失效条件 / 重新立项条件**——决定是在特定认知下做的，认知变了决定就该重开
3. 理由的**排列顺序有意义**：把决定性的理由放第一位，因为它最可能被后来者只读到
4. 多处提及同一结论时**全部同步**，否则文档内部矛盾
5. 每轮结束给一句明确的**可实施判断**，不要让状态停在「讨论过了」

### 4.2 「不做」也要登记

**定义**：被否决的方案不删除，进「不做的事项」表并附理由。

**内涵**：一个被否决的方案如果没有留下理由，下一个人（或下一次会话的你自己）会重新提出它，然后重新走一遍否决流程。

**说明性示例**：一张「不做的事项」表通常汇集了几个来源的条目：

- 来自最初过滤的：过早扩张的平台/功能支持、非核心的补全能力、第三方库替换
- 来自评审移除的：若干个被判定 YAGNI / 自纠正 / 比问题更危险的任务（各附完整理由）
- 来自方案收敛的：几条被否决的替代实现——**每条都注明「经实测」**（例如某结构化改写方案会丢失注释、重排结构、其错误处理形同虚设；某「更省事版本」会改坏正文；某字符收窄会产出非法格式）
- 来自纯测试任务的：明确「只记录、不修」的已知缺陷
- 来自最小改动原则的：明确「不顺带修」的相邻既有问题

**如何复用**：
1. 表格两列足够：建议项 / 不做理由
2. 理由里要包含**实测证据**（「经实测 X 会导致 Y」），而不只是价值判断
3. 如果否决是基于当前认知，注明失效条件
4. 这张表的长度是评审质量的指标——**只增不减的方案说明没人真的在质疑它**

### 4.3 钉住缺陷的测试必须自我标注

**定义**：特征测试里锁定**已知缺陷行为**的用例，测试名必须以 `KNOWN DEFECT (编号):` 开头，并在测试体内注明「本条钉住的是当前缺陷行为、不是期望行为，将来修复时应**反转**断言而非删除」。

**内涵**：一条常绿的测试会被后来者读成对该行为的设计背书。不标注，就是把「我们决定暂时不修」悄悄变成了「这是正确行为」。

**说明性示例**：一组特征测试中若有钉住已知缺陷的用例，其命名与注释应做到：

- 名称带缺陷编号，如 `... KNOWN DEFECT (编号): <描述当前被钉住的缺陷行为>`
- 注释写明**同源关系与修法方向**，如「与某缺陷同源；将来按某种方式修复本缺陷时，应**反转**本条断言」

**如何复用**：
1. 编号要与缺陷清单对得上，让测试和文档可以互相索引
2. 注释里写清**修复时该怎么做**（反转断言），而不只是「这是 bug」
3. 纯测试任务的验收标准：所有用例首跑即全绿；**若首跑出现红，说明期望值抄错了，应回到实测输出核对，而不是改源码让它变绿**

---

## 第 5 章 工程纪律层

### 5.1 边界保护与零侵入

**定义**：约定「先别动当前项目」时，全程只改约定范围内的文件；**每一轮操作结束都要验证边界没被突破**。

**说明性示例**：整个评审阶段（可能跨多轮会话）项目源码**零修改**：

- 每轮结束执行 `git status --short`，必须为空
- 临时文件计数必须为 0（用统一前缀，如 `.tmp-*`，便于一键统计与清理）
- 所有验证通过 import 真实源码 + 临时文件完成，**不落任何改动到源码、测试、构建产物、依赖清单、变更日志**
- 构建产物零漂移是一条独立验收项

**如何复用**：
1. 约束要写成**可执行的检查**（一条命令 + 期望输出），不要写成「注意不要改」
2. 每轮结束跑一次，而不是最后跑一次
3. 临时文件用统一前缀，便于一键计数与清理

### 5.2 原子提交与粒度判断

**定义**：提交按**逻辑原子性**拆分，不是按文件数或代码行数拆分。判据是：这个提交单独 checkout 出来时，代码库是否处于一个**说得通的状态**。

**说明性示例**：

| 场景 | 决定 | 理由 |
|---|---|---|
| 同一处解析循环的等号支持 + 相关 bug 修复 + 护栏 | **合为一个** `feat` 提交 | 三者是同一次重写；拆开需要构造一个「已支持新语法但某分支仍丢值」的人为中间态，**那个状态本身是坏的** |
| 功能改动 + 配套纯文档 | **拆成两个**（`feat`/`ci` 与 `docs`） | 功能改动与纯文档分开 |
| 实现 + 验证记录文档 | **拆成两个** | 同上 |
| 纯测试任务 | 一个 `test` 提交 | 只触达测试与变更日志，源码与构建产物零漂移 |
| 扩容后暴露的平台修复 | 独立 `fix(test)` | 它是扩容**暴露**的问题，不是扩容本身的一部分 |

**Commit message 规范**（通用约定）：

- 采用 Conventional Commits 带 scope：`feat(scope):` / `fix(scope):` / `ci:` / `docs:` / `test(scope):`
- 详细描述写：**变更内容 + 影响范围 + 验证结果**（实测数字、门禁退出码、构建产物是否零漂移）
- 提交前先更新变更日志，相关改动**合并为一条记录**（不按提交拆分）
- 变更日志条目结构：`**日期 · 类型 · 标题**` + 背景 / 变更 / 测试（必要时加「顺带修复」「不做」）
- **默认仅提交不推送**；禁止自动提交，等待明确指示

**最小原子改动的具体含义**（示例）：

- 最小版只改**一个叶子函数**，不碰被全链路使用的共享解析器
- 纯测试任务里，版本状态中出现任何源码或构建产物改动都意味着越界
- 明确**不**顺带修某个相邻的既有行为——那是既有语义，扩大它会突破最小原子改动边界
- 明确**不**顺带收紧某处判据——记入「不做」表另行评估

**如何复用**：
1. 拆分前先问：「中间态是不是一个坏状态？」如果是，就不要拆
2. commit body 里写**实测数字**，不写「应该没问题」
3. 类型选择看主要意图：主体是新功能就选 `feat`，顺带修的 bug 在 body 里说明
4. 「顺带能做」不等于「应该顺带做」；每一个顺带项都要单独过 §1.3 与 §2.1

### 5.3 工具坑的即时识别与切换

**定义**：工具报错时，先判断是「我的用法错了」还是「这个工具在这个环境里不可靠」，然后**切换工具**而不是反复重试。

**常见工具坑（以 Windows + PowerShell 5.1 为例，具通用性）**：

| 工具坑 | 症状 | 切换方案 |
|---|---|---|
| 读取原始内容的字符/行计数不可靠 | 对 UTF-8-no-BOM 文件报出的计数与实际不符 | 改用运行时精确核验（字符数 / CRLF / 孤立 LF / 行数 / BOM） |
| 内联 `node -e "..."` | 引号 / 反引号被 shell 转义吃掉 → 语法错误 | 改写成临时脚本文件执行 |
| 脚本文件被 shell 以错误编码读取 | 脚本里的非 ASCII 字面量（如中文路径）变乱码 → 「路径含非法字符」 | **不要在脚本里硬写非 ASCII 字面量**；改用文件系统枚举（如按扩展名 `Get-ChildItem`）定位目标，或以当前工作目录 + 相对路径规避 |
| 编辑工具无法写工作区外文件 | 报权限/范围错误 | 工作区内临时脚本 + 提权执行 |
| 管道过滤丢内容 | 过滤工具输出时漏掉部分行（编码破坏） | 让工具用 `--output=` 写文件，再用运行时解析 |
| 版本控制命令被沙箱拒绝 | 「拒绝访问」 | 单独提权重试 |
| 控制台输出非 ASCII 乱码 | 计数结果被乱码淹没 | 只依赖退出码与写文件的结构化输出 |

**如何复用**：
1. 同一个命令**失败两次**就换方法，不要第三次
2. 在编码敏感的环境里，涉及非 ASCII / 精确字节计数的验证一律走可靠的运行时，而非 shell 内建
3. 需要解析工具输出时，让工具**写文件**而不是走管道
4. 把踩到的坑记进长期记忆——这类条目会在后续会话里多次省下重复排查

---

## 第 6 章 操作清单

### 6.1 评审一个候选任务时，按顺序回答

1. **问题真的存在吗？** 有没有人观测到？还是只是看起来不优雅？
2. **如果存在，严重吗？** 修好它，用户能观测到什么变化？（追到数据生命周期：用户资产还是一次性产物？）
3. **改动面多大？** 能否限制在叶子函数内？触达几个文件、几条调用链？
4. **收益与代价成比例吗？** 审美性收益配得上这个改动面吗？
5. **原方案的实现正确吗？** 有没有比它要修的问题更危险的地方？
6. **决定不做的话，理由和重新立项条件写在哪？**

### 6.2 采纳一份「重写某函数」的方案前

1. 从文档**逐字抽取**新旧实现（不手抄），抽取后断言关键标识与条数
2. 构造 fixture 集，**必须包含现有测试的全部输入**
3. 跑差分，报告「相同 / 改变」两个计数
4. 改变项逐条归类：意图内修复 / 意外变更 / **既有缺陷被顺带修好**（这类要补护栏测试，否则是未登记的静默行为变更）
5. 新测试分别对旧实现和新实现各跑一次，取得**精确红绿条数**写回文档
6. 文档改完后**重新抽取再跑一次**，证明文档本身可执行正确
7. 用版本控制工具的 `diff` 核对 hunk 数与替换次数一一对应

### 6.3 批量编辑文档时

1. 先备份
2. 每处替换的目标串断言「恰好出现 1 次」
3. 替换串**一律用回调** `() => newStr`
4. 计数断言 + 行数护栏 + 结构完整性（围栏偶数、无 CR/BOM、标题数不变）
5. **校验置于写盘之前**
6. 写盘后做行级差分，确认没有意外改动
7. 回读关键区段，确认渲染后的形态与意图一致
8. 删除临时文件，确认版本控制状态干净

### 6.4 提交前

1. 变更日志先更新（相关改动合并为一条）
2. 四步门禁：类型检查 / 静态检查（lint）/ 测试 / 构建，退出码均为 0
3. `git status --short` 检查改动面是否符合任务声明（构建产物零漂移）
4. Conventional Commits + scope；body 写变更 / 影响范围 / **实测验证结果**
5. 等待明确指示再提交；默认不推送

---

## 第 7 章 反模式清单（实际踩过的）

| 反模式 | 说明性实例 | 后果 |
|---|---|---|
| 用「逐条核验过了」代替实跑 | 声称「现有用例全部继续通过（逐条已核验）」 | 声称本身可能是对的，但**没有发现遗漏的输入类别**——核验只覆盖了它想到的地方 |
| 只核实一条链路就断言不可达 | 只看文本视图、没看解析视图就判「当前不可达」 | 判断错误，且可能被用于支撑范围决定 |
| 用成本论证该不该做 | 「这个缺陷只要几行就能修」 | 差点做了不产生用户可见收益的改动 |
| 悄悄修改错误表述 | ——（**没做**，选择引用后驳斥） | 若做了，后来者会失去「这里曾经错过」的信息 |
| 机械套用红/绿两阶段 | ——（纯测试任务明确不套用） | 纯测试任务会凭空造出一个「失败」阶段 |
| 替换串直接传入字符串替换 API | 编辑文档时 `` $` `` 被展开 | 文档头部被复制多遍，多项断言以整数倍超标 |
| 自己写行对齐算法做差分 | 报出假的增删行数 | 差点误判某段内容被改动 |
| 内联脚本里放引号/反引号 | shell 吃掉转义 | 语法错误，反复踩 |
| 脚本里硬写非 ASCII 字面量 | 被 shell 以错误编码读取 | 中文路径变乱码 →「路径含非法字符」 |
| 给不可达路径写测试 | ——（确认不可达后砍掉） | 若写了，会把投机行为固化成规格 |
| 缺陷测试不自我标注 | ——（用 `KNOWN DEFECT` 前缀） | 若不做，常绿测试会被读成设计背书 |
| 顺带修无关问题 | ——（不顺带修相邻既有行为、不顺带收紧判据） | 若做了，会突破最小原子改动边界、模糊验收标准 |
| 让决定停在「可选延伸」 | ——（改写为「已决定：不做」+ 重新立项条件） | 若不做，悬而未决会被下一个人重新打开 |

---

## 附录 A 项目账目应记录什么

方法论落地后，建议为每个项目维护一份「最终账目」，用可核对的事实替代形容词。它通常包含三张表：

**① 方案文档演进**：记录每轮编辑后的规模与编辑处数，以及一致性保证。

| 阶段 | 规模（行数/字节） | 本轮编辑处数 |
|---|---|---|
| （示例）某轮收敛 + 某任务移除后 | —— | —— |
| （示例）写入某新增任务 | —— | 若干处 |
| （示例）记录某决定 | —— | 若干处 |

> 全程保持统一的换行/编码约定（如纯 LF、无 BOM）；每轮均以版本控制工具的 `diff` 核对 hunk 数与替换次数一一对应；每轮结束项目仓库 `git status --short` 为空。

**② 任务实施状态**：逐任务记录「保留/移除/收敛/新增」的去向、是否已实施、以及对应的提交标识（在内部版本中保留，通用文档中以占位符表示）。

**③ 剩余工时与依赖约束**：记录尚未实施任务的预估工时，以及任务间的**顺序依赖**（例如「某纯测试任务复用另一任务引入的 helper，顺序不可颠倒」；而与之无依赖的任务可独立实施）。

> 说明：本附录在通用版中**不填具体数字与提交号**——它们属于单个项目的一次性事实，应记录在该项目自己的方案文档里，并可回溯核对。

---

## 附录 B 一句话版

> **能跑的就必须跑，跑出来的就照实说，说错了就当场改并且告诉依赖它的人，决定不做就写下理由和重开条件，改完就验证边界没被突破。**

---

*本文档为跨项目通用版：已剥离具体项目的提交号、文件名、行号、任务编号与精确计数，将其改写为说明性示例。方法论内核（原则、决策逻辑、验证步骤、反模式）与源项目一致。*
