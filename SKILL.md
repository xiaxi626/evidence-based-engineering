---
name: evidence-based-engineering
description: "An evidence-based engineering discipline for verification and collaborative decision-making. Use when reviewing a proposed task/plan/PR, before claiming 'zero regression' / 'verified' / 'should be fine', when judging whether a bug is worth fixing, when assessing code reachability, when writing characterization tests for known defects, when batch-editing documents, when deciding atomic-commit granularity, or when turning an external audit/evaluation into an implementation plan. 中文触发场景：评审候选任务或方案、声称零回归或已核验之前、判断缺陷该不该修、可达性判定、为已知缺陷写特征测试、批量编辑文档、原子提交粒度、决策闭环、把外部评估转化为实施计划。The core is to turn 'I think' into 'measurement shows' via real-code probes, fixture diffing, count-based self-checks, severity-over-cost judgment, and a won't-do register."
---

# Evidence-Based Engineering · 实证工程方法论

> **一句话内核**：能跑的就必须跑，跑出来的就照实说，说错了就当场改并告诉依赖它的人，决定不做就写下理由和重开条件，改完就验证边界没被突破。

本技能把「我认为」尽快变成「实测显示」。它是**默认工作法，不是审批关卡**——目的是降低返工，而非增加流程负担。可操作的速查清单在下方；详细原理、说明性实证示例与完整反模式清单见 [reference.md](reference.md)。

## 何时使用

- 评审一个候选任务 / 方案 / PR 时
- 准备写下「零回归」「已核验」「应该没问题」之前
- 判断一个缺陷该不该修、值不值得修时
- 做「某路径不可达」这类可达性判定时
- 为已知缺陷写特征测试（characterization test）时
- 批量编辑文档、需要保证编辑可失败可回滚时
- 决定提交粒度（原子提交）时
- 想把一份外部评估 / 审计转化为实施计划时

## 七条核心原则（速查）

1. **实测优于推断**：能跑就必须跑；结论要带计数，不用形容词。错误往往集中在自信的地方。
2. **真实代码驱动验证**：探针 import 真实实现，禁止复制逻辑；文档里的代码逐字抽取，不手抄。
3. **问题存在性优先**：先问「问题真的存在吗、有人观测到吗」，再问「怎么修」。顺序不可颠倒。
4. **严重性与成本分开**：该不该修取决于「修好是否改变用户可见结果」，不是「修它多便宜」。
5. **最小化风险**：大改动前先写探针止损；「改动能否限制在叶子函数内」是核心判据；共享基础设施默认不动。
6. **自我校正与主动披露**：证据推翻自己时，先告知依赖该判断的人，再修正；更正要留痕（引用后驳斥，不悄悄改）。
7. **决策闭环**：每个悬而未决都要落成「做 / 不做」并写理由与重新立项条件；「不做」也登记进表。

## 清单 1 · 评审一个候选任务时（按顺序回答）

1. 问题真的存在吗？有人观测到，还是只是看起来不优雅？
2. 如果存在，严重吗？修好它用户能观测到什么变化？（追到数据生命周期：用户资产还是一次性产物？）
3. 改动面多大？能否限制在叶子函数内？触达几个文件、几条调用链？
4. 收益与代价成比例吗？审美性收益配得上这个改动面吗？
5. 原方案的实现正确吗？有没有比它要修的问题更危险的地方？
6. 决定不做的话，理由和重新立项条件写在哪？

## 清单 2 · 采纳「重写某函数」方案前

1. 从文档逐字抽取新旧实现（不手抄），抽取后断言关键标识与条数。
2. 构造 fixture 集，**必须包含现有测试的全部输入**。
3. 跑差分，报告「相同 / 改变」两个计数。
4. 改变项逐条归类：意图内修复 / 意外变更 / 既有缺陷被顺带修好（后者要补护栏测试，否则是未登记的静默行为变更）。
5. 新测试分别对旧、新实现各跑一次，取得精确红绿条数写回文档。
6. 文档改完后重新抽取再跑一次，证明文档本身可执行正确。
7. 用版本控制工具的 `diff` 核对 hunk 数与替换次数一一对应。

## 清单 3 · 批量编辑文档时

1. 先备份。
2. 每处替换的目标串断言「恰好出现 1 次」。
3. 替换串一律用回调 `() => newStr`（防 `$&` / `` $` `` 等反向引用被展开）。
4. 计数断言 + 行数护栏 + 结构完整性（围栏偶数、无 CR/BOM、标题数不变）。
5. **校验置于写盘之前**——让所有失败都成为无损失败。
6. 写盘后做行级差分（用 git，不用自写算法），确认无意外改动。
7. 回读关键区段，确认渲染形态与意图一致。
8. 删除临时文件，确认版本控制状态干净。

## 清单 4 · 提交前

1. 变更日志先更新（相关改动合并为一条）。
2. 四步门禁：类型检查 / lint / 测试 / 构建，退出码均为 0。
3. `git status --short` 检查改动面是否符合任务声明（构建产物零漂移）。
4. Conventional Commits + scope；body 写变更 / 影响范围 / 实测验证结果。
5. 等待明确指示再提交；默认不推送。

## 决策尺子（三条最常用的判断）

- **严重性 > 成本**：「只要几行」不是理由。先独立回答「修好它用户能观测到什么变化」与「修它要动多少东西」；若前者是「无变化」，后者再便宜也不做。决定不做必须写重新立项条件。
- **可达性按两条独立链路判定**：解析视图决定是否被调用，文本视图决定改到哪一行；只核实一条会误判。主张「不可达」必须指出是哪个不变量保证的、它在代码里的位置；理由只是「没见到过」= 低频，不是不可达。
- **缺陷测试自我标注**：钉住已知缺陷行为的用例，测试名以 `KNOWN DEFECT (编号):` 开头，注明「将来修复时应反转断言而非删除」。否则常绿测试会被读成设计背书。

## 把外部评估变成实施计划（推导链）

拿到一份外部评估 / 审计时，不要照单全收，走四步：① 逐条到仓库核实 → ② 过滤三类内容（事实性错误 / 过早扩张 / 违反硬约束）→ ③ 剩下的收敛为最小任务 → ④ 被过滤项进「不做的事项」表并附理由。核查不是对作者的不信任，是方案应有的最后一道工序——一份看似合理的计划，逐任务核查后往往有相当比例需被移除、收敛、打补丁或新增。完整背景与示例见 [reference.md](reference.md) 第 0 章。

## 反模式速记（详见 reference.md）

用「逐条核验过了」代替实跑 · 只核实一条链路就断言不可达 · 用成本论证该不该做 · 悄悄改错误表述 · 机械套用红/绿两阶段 · 替换串直接传入字符串替换 API · 自写行对齐算法做差分 · 内联脚本放引号/反引号 · 脚本里硬写非 ASCII 字面量 · 给不可达路径写测试 · 缺陷测试不自我标注 · 顺带修无关问题 · 让决定停在「可选延伸」。
